# Sample code

Snippets that reflect the style and patterns used in GradeX (simplified for showcase). The live app codebase is not open-sourced.

- **`components/`** — React components (hooks, responsive layout)
- **`api/`** — Node/Express-style API handlers
- **`lib/`** — Small utilities
